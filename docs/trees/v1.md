gluceel-web — شجرة المشروع (v1)
ملف: docs/trees/v1.md

نسخة: v1
تاريخ الاعتماد: 2025-10-25 (Asia/Kuwait)
نطاق: أسماء الصفحات + ربط المسارات (No-Code)

قواعد سريعة:

جميع المسارات المحمية تحت ‎/app‎ بعد تسجيل الدخول.

معرّفات ديناميكية قياسية: ‎:childId‎، ‎:visitId‎، ‎:itemId‎.

صيغة الأسماء: عناوين عربية مفهومة، والمسارات بالإنجليزية snake/kebab.

الشجرة (UI ↔ route):

Auth

Login ↔ /auth/login

Register ↔ /auth/register

Forgot Password ↔ /auth/forgot-password

Reset Password ↔ /auth/reset-password

App Shell

Dashboard (Role-aware) ↔ /app

Switch Role ↔ /app/switch-role

Profile & Preferences ↔ /app/profile

Notifications Center ↔ /app/notifications

Children (Guardian)

Children List ↔ /app/children

Add Child ↔ /app/children/new

Child Overview ↔ /app/children/:childId/overview

Child Glucose ↔ /app/children/:childId/glucose

Child Meals ↔ /app/children/:childId/meals

Child Diet Plan ↔ /app/children/:childId/diet-plan

Child Visits ↔ /app/children/:childId/visits

Child Attachments ↔ /app/children/:childId/attachments

Child Devices ↔ /app/children/:childId/devices

Child Analytics ↔ /app/children/:childId/analytics

Provider — Doctor

Doctor Panel ↔ /app/doctor

Assigned Children ↔ /app/doctor/children

Visit Calendar ↔ /app/doctor/visits

Visit Details ↔ /app/doctor/visits/:visitId

Provider — Dietitian

Dietitian Panel ↔ /app/dietitian

Assigned Children ↔ /app/dietitian/children

Diet Plans ↔ /app/dietitian/diet-plans

Child Diet Plan (via provider) ↔ /app/dietitian/children/:childId/diet-plan

Catalogs

Food Items ↔ /app/catalog/food-items

Food Item Details ↔ /app/catalog/food-items/:itemId

New Food Item ↔ /app/catalog/food-items/new

Food Categories ↔ /app/catalog/categories

Device Catalog ↔ /app/catalog/devices

Admin

Users & Roles ↔ /app/admin/users-roles

Linking: Doctor ↔ Children ↔ /app/admin/linking/doctor-children

Linking: Dietitian ↔ Children ↔ /app/admin/linking/dietitian-children

Help / About (اختياري)

Help Center ↔ /app/help

ملاحظات توافق:

تغطي أقسام Children وCatalogs وProviders وAdmin جميع الكيانات المعتمدة في db-schema v1.2 (children, visits, food_items, diet_plans, الربط doctor_children/dietitian_children، الأجهزة).

المعرفات الديناميكية ثابتة على مستوى الشجرة وتُستخدم عبر كل الصفحات ذات العلاقة.



gluceel-web — Tree (v1)

أسماء مجلدات فقط، بدون أي ملفات. أي Rename/Delete لاحقًا يكون بتذكرة منفصلة.
هذه النسخة v1 تضم: خريطة المسارات ↔ هيكل الشجرة + ربط المسارات ببيانات القاعدة (Data Links).

1) Routes ↔ Tree map (v1)

app/
auth/
dashboard/
children/
child/
[childId]/
overview/
readings/
meals/
new/
[mealId]/
visits/
new/
[visitId]/
plans/
diabetes/
[planId]/
diet/
[planId]/
files/
[fileId]/
food-items/
new/
[id]/
meals/ # (اختياري: عرض مجمّع)
visits/ # (اختياري: عرض مجمّع)
analytics/
doctor/
child/
[childId]/
overview/
readings/
meals/
visits/
dietitian/
child/
[childId]/
overview/
meals/
plans/
admin/

Arabic Labels (UI)

/auth → تسجيل الدخول

/dashboard → الرئيسية

/children → الأطفال

/child/:childId/* → ملف الطفل (نظرة عامة • قياسات • وجبات • زيارات • خطط • ملفات)

/food-items/* → الأصناف الغذائية

/meals, /visits → (لوحات مجمّعة اختيارية)

/analytics → التحليلات

/doctor/* → لوحة الطبيب

/dietitian/* → لوحة أخصائي التغذية

/admin → الإدارة

2) Data Links (v1)

/auth ↔ profiles, roles, user_roles

/dashboard ↔ children (ملخصات), growth_metrics (آخر قياس), analytics_daily (اختياري)

/children ↔ children (CRUD للولي), growth_metrics (عرض آخر قياس)

/child/:childId/overview ↔ children, growth_metrics, visits(آخر), diet_plans(أحدث حالة), attachments(count)

/child/:childId/readings ↔ glucose_readings, insulin_doses (عرض سياقي), meals (ربط related_meal_id)

/child/:childId/meals[/*] ↔ meals, meal_items, food_items, food_portions, (اختياري) insulin_doses.linked_bolus_id

/child/:childId/visits[/*] ↔ visits, visit_recommendations, attachments

/child/:childId/plans[/*] ↔ diet_plans, diet_plan_meals, child_diabetes_profiles, (اختياري) carb_* / basal_* / mdi_basal_*

/child/:childId/files[/*] ↔ attachments (Bucket: attachments)

/food-items[/*] ↔ food_items, food_categories, food_portions (Bucket: food-items)

/meals (مجمّع) ↔ meals (by child filters), meal_items

/visits (مجمّع) ↔ visits (by child/provider filters)

/analytics ↔ analytics_daily (+ تجميعات من meals/meal_items/glucose_readings)

/doctor/child/:childId/ ↔ doctor_children, visits (CRUD للدكتور), glucose_readings (قراءة), meals (قراءة)

/dietitian/child/:childId/ ↔ dietitian_children, diet_plans / diet_plan_meals (CRUD للأخصائي), meals/meal_items (قراءة)

/admin ↔ profiles, roles, user_roles, doctor_children, dietitian_children, food_categories

القاعدة الذهبية: الواجهة تظهر/تخفي القدرات حسب الدور، لكن المنع الحقيقي يتم عبر RLS في قاعدة البيانات.
[[END]]
